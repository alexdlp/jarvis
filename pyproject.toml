[project]
name = "jarvis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AlexNitid", email = "alex.delapuente@nitid.ai" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp>=2.1.1",
]

[project.scripts]
jarvis = "jarvis:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
